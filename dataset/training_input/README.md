Here lie all the datasets that will be used as input.

We divide these into to folders. `internal/` and `external/`.

The reason behind this is because we want to make the separation between these two groups clear.

So we can later add more importance to the internal data.
